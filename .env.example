# Moltboard Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================
# MongoDB Configuration
# ============================================
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/moltboard?retryWrites=true&w=majority

# ============================================
# GitHub OAuth (NextAuth)
# ============================================
# Create a GitHub OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Comma-separated list of allowed GitHub usernames
ALLOWED_GITHUB_USERS=your_username,collaborator_username

# ============================================
# Google OAuth (NextAuth) - Optional
# ============================================
# Create credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID (Web application)
# 2. Add redirect URIs:
#    - http://localhost:3001/api/auth/callback/google (dev)
#    - https://moltboard.app/api/auth/callback/google (prod)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Comma-separated list of allowed Google emails
ALLOWED_GOOGLE_EMAILS=melissa@example.com,friend@gmail.com

# ============================================
# NextAuth Configuration
# ============================================
# For development:
NEXTAUTH_URL=http://localhost:3001

# For production, set to your domain:
# NEXTAUTH_URL=https://moltboard.app

# Generate a random secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your_random_secret_key_here

# ============================================
# Moltboard API
# ============================================
# API key for Moltbot authentication (used via x-api-key header)
# Generate a secure random key
MOLTBOARD_API_KEY=your_secure_api_key_here

# ============================================
# Optional: Vercel Configuration
# ============================================
# These are auto-populated by Vercel, but you can set them for local dev
# VERCEL=1
# VERCEL_ENV=development
# VERCEL_URL=

# ============================================
# Optional: OpenClaw Integration
# ============================================
# Uncomment when building the OpenClaw channel adapter
# OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789
# OPENCLAW_CHANNEL_ID=moltboard

# ============================================
# Clawdbot Webhook Integration (Push Model)
# ============================================
# Enable webhook push to Clawdbot instead of Clawdbot polling Moltboard
CLAWDBOT_WEBHOOK_ENABLED=true
# URL of Clawdbot's webhook endpoint (usually gateway:18789 + path)
CLAWDBOT_WEBHOOK_URL=http://localhost:18789/webhook/moltboard
# Shared secret for webhook signature verification (optional but recommended)
# Generate with: openssl rand -base64 32
CLAWDBOT_WEBHOOK_SECRET=your_webhook_secret_here
